!!!
%html
  %head
    = render(partial: "shared/head")

  %body
    .w-full.mx-auto.lg:container
      = render(partial: "shared/header")
      = render(partial: "shared/alerts")

      - if signed_in? && current_account.bot_user.blank?
        = render(partial: "shared/bot_auth")

      .px-2
        = yield
