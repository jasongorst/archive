- Administrate::Engine.stylesheets.each do |css_path|
  = stylesheet_link_tag css_path, data: { turbo_track: "reload" }

= stylesheet_link_tag "color_picker_field", data: { turbo_track: "reload" }
= yield :stylesheet
