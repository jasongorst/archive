- cache [@channel, year, month, @message_dates_with_counts] do
  - dates_in_month(month, year, @message_dates_with_counts).each do |date, count|
    = render(partial: "date",
      locals: { date: date, count: count },
      cached: [@channel, date, count])
