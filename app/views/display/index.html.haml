.display
  .page-header
    %h1 Channels

  %ul.nav.flex-column
    - @channels.each do |channel|
      - unless channel.messages.empty?
        %li.nav-item
          = link_to("\##{channel.name}", channel, class: 'nav-link pl-0')
