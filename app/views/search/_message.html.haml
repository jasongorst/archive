- # message[0] is the url to the message in display#show with id and page
- # message[1] is the result returned by Message.search
- url = message[0]
- message = message[1]
%p
  %div
    %small.text-muted= "\##{message.channel.name}" if params[:search][:channel_id].empty?
  .message.position-relative
    %div
      %a.text-reset.stretched-link{href: url}
        %strong
          %span{style: "color: #{message.user.color}"}
            = message.user.display_name
        %span= Time.at(message.ts).strftime("%b %-e, %Y at %-l:%M%P %Z")
    %div!= message.excerpts.text
