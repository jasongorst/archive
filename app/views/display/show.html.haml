- cache @channel do
  %h1.text-lg.lg:text-2xl.pl-6.pb-2.lg:pb-3
    \##{@channel.name}

  - @message_counts_by_date.each do |year, months|
    - months.each do |month, dates|
      = render(partial: "month",
        locals: { year: year, month: month, dates: dates },
        cached: [@channel, year, month, ActiveSupport::Digest.hexdigest(dates.to_s)])
