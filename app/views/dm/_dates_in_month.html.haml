- cache [@private_channel, year, month, @private_message_dates_with_counts] do
  - dates_in_month(month, year, @private_message_dates_with_counts).each do |date, count|
    = render(partial: "date",
      locals: { date: date, count: count },
      cached: [@private_channel, date, count])
